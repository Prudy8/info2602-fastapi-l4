$kind: http-request
url: "{{baseUrl}}/todos"
method: GET
headers:
  Accept: application/json
auth:
  type: oauth2
  credentials:
    accessTokenUrl: token
    grant_type: password_credentials
examples: ./.resources/Get Todos.resources/examples
order: 1000
