$kind: http-request
name: Update Todo
url: "{{baseUrl}}/todo/:id"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  id: "9312"
auth:
  type: oauth2
  credentials:
    accessTokenUrl: token
    grant_type: password_credentials
examples: ./.resources/Update Todo 1.resources/examples
order: 3000
